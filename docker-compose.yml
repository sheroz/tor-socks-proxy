services:
  tor-socks-proxy:
    container_name: tor-socks-proxy
    build:
      context: .
    image: tor-socks-proxy
    ports:
      - "127.0.0.1:8853:53/udp"
      - "127.0.0.1:9150:9150/tcp"
    restart: unless-stopped